on: pull_request

jobs:
  push_from_fork:
    runs-on: ubuntu-latest
    name: push_from_fork
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.head.sha }}
